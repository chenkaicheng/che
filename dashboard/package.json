{
  "name": "che-dashboard",
  "version": "4.0.0",
  "license": "EPL-1.0",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "postinstall": "npm run typings && npm run build",
    "typings": "typings install",
    "build": "gulp build"
  }
}
